spring:
  application:
    name: user-service
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 5000
            readTimeout: 120000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc.batch_size: 100
        order_inserts: true
        order_updates: true

server:
  port: 8081

app:
  internal:
    secret: ${INTERNAL_SERVICE_SECRET:dev-local-internal-secret}
  student:
    email-domain: ${STUDENT_EMAIL_DOMAIN:student.edu.vn}
  student-face:
    storage-provider: ${STUDENT_FACE_STORAGE_PROVIDER:local}
    upload-dir: ${STUDENT_FACE_UPLOAD_DIR:uploads/student-faces}
    s3-bucket: ${STUDENT_FACE_S3_BUCKET:}
    s3-region: ${AWS_REGION:ap-southeast-1}
    s3-prefix: ${STUDENT_FACE_S3_PREFIX:student-faces}
    rekognition-region: ${AWS_REKOGNITION_REGION:ap-southeast-1}
    rekognition-threshold: ${AWS_REKOGNITION_THRESHOLD:90}
    max-size-bytes: ${STUDENT_FACE_IMAGE_MAX_SIZE_BYTES:5242880}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
